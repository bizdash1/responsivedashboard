<!DOCTYPE html>
<html>
    <head>
        <title> <%= title %> </title>
        <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <link href="https://fonts.googleapis.com/css?family=Roboto:100,400" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="/css/home.css">
    </head>
    <body>
        <div class="navbar">
            <a href="/users/signup"><i class="fa fa-fw fa-user-plus"></i> SignUp</a>
            <a href="/users/signin"><i class="fa fa-fw fa-user"></i> SignIn</a>
            <a class="active" href="#"><i class="fa fa-fw fa-home"></i> Home</a>
        </div>

        <div class="bgwrap">
            <video id="bgvidelement" preload="auto" autoplay="true" loop="loop" muted="muted">
                <source src="https://dl.dropboxusercontent.com/s/of8i6ft66ugwfjd/Blurry-Lights.mp4?dl=0" type="video/mp4">
            </video>
        </div>
          
        <div class="content">
            <h1 id="welcome"> Biz-Dashboard </h1>
            <h2> Your personalized Business Dashboard </h2>
        </div>

        <script>
            <% if (flash.success && flash.success.length>0) { %>
                new Noty({
                    theme : 'relax',
                    text : '<%=flash.success%>',
                    type : 'success',
                    layout : 'topRight',
                    timeout : 1500
                }).show();
            <% } %>
            <% if (flash.error && flash.error.length>0) { %>
                new Noty({
                    theme : 'relax',
                    text : '<%=flash.error%>',
                    type : 'error',
                    layout : 'topRight',
                    timeout : 1500
                }).show();
            <% } %>
        </script>
    </body>
</html>